node_modules/
# Ignore generated render output (PDF, PPTX) but DO track committed Pages assets in out/thumbs and out/zeroheight-pages
out/*.pdf
out/*.pptx
out/*.png
.DS_Store
*.log
# Local secrets — Zeroheight tokens, etc.
.env
.env.local
# Never commit raw bundle dumps
/tmp_bundle/
package-lock.json
raw/
.memsearch/
