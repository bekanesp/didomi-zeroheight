node_modules/
out/
# But DO ship slide thumbnails + Zeroheight page drafts (referenced from Zeroheight iframes / pages)
!out/thumbs/
!out/thumbs/**
!out/zeroheight-pages/
!out/zeroheight-pages/**
.DS_Store
*.log
# Local secrets — Zeroheight tokens, etc.
.env
.env.local
# Never commit raw bundle dumps
/tmp_bundle/
package-lock.json
raw/
.memsearch/
